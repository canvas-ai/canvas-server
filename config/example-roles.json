{
    "ollama": {
        "type": "local",
        "name": "Ollama",
        "description": "Ollama",
        "driver": "docker",
        "driverOptions": {
            "dockerCompose": "docker.ollama/docker-compose.yml"
        }
    }
}
