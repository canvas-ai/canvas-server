import debugMessage from 'debug';
const debug = debugMessage('canvas:neurald');

// Power law
// Scale invariance
// 1000 brains
// Universe is a huge resonance chamber

export default class NeuralD {
    constructor(options = {}) {}
}
